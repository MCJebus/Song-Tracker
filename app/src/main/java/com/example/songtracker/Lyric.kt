package com.example.songtracker

class Lyric(id: Int, line: String, song: String, artist: String, found: Boolean) {
    var id: Int? = id
    var lyric: String = line
    var song: String = song
    var artist: String = artist
    var found: Boolean = found
}